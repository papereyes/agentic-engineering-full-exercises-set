# Legacy Access Workflow Description

This document is retained from the first release and must not be updated.

All requests move from draft to manager approval and directly to data-owner approval. Security review happens outside this application and does not affect routing.

Provisioning failures retry automatically until access is granted. Rollback and identity-administrator actions are outside the application, so the only completed workflow state is provisioned.

Compare every claim with the implementation and record each contradiction.
