Create three Mermaid diagrams for the access-provisioning workflow described below:

- `diagrams/access-state.mmd`: the complete state workflow
- `diagrams/access-approval-sequence.mmd`: normal and high-risk approval routing
- `diagrams/access-failure-sequence.mmd`: provisioning failure and rollback behavior

This is a first attempt. Use only the supplied legacy description as the behavioral authority. Do not inspect application source, tests, trace scripts, protected contracts, or existing diagrams. Do not ask follow-up questions or retry. Write only the three requested diagram files.

# Legacy Access Workflow Description

This document is retained from the first release and must not be updated.

All requests move from draft to manager approval and directly to data-owner approval. Security review happens outside this application and does not affect routing.

Provisioning failures retry automatically until access is granted. Rollback and identity-administrator actions are outside the application, so the only completed workflow state is provisioned.

Compare every claim with the implementation and record each contradiction.
