# Evidence Index

This folder holds the before/after comparison and the real evidence pack
generated by `pr-evidence-app/scripts/generate-pr-evidence.mjs` at
implementation commit `2f719be07e54362969a43abf20f8ab22b9c3b111`.

## What's here

- `before.md` / `before.patch` — the naive, unguided first attempt (branch
  `exercise-08-01-before`).
- `after.md` / `after.patch` — the spec-driven first attempt (branch
  `exercise-08-01-after`, this branch).
- `comparison.md` — before/after proof comparison.
- `generated/pr-evidence.json`, `generated/summary.md`,
  `generated/artifacts/` — the real evidence pack produced by running the
  generator against `fixtures/check-results.json` with
  `--sha 2f719be07e54362969a43abf20f8ab22b9c3b111`.
- `commands/` — captured raw command output.

## How to reproduce

From `pr-evidence-app/`:

```
node ./scripts/generate-pr-evidence.mjs \
  --fixture ../fixtures/check-results.json \
  --sha 2f719be07e54362969a43abf20f8ab22b9c3b111 \
  --output ../evidence/generated
```

## What the generated pack proves

Source SHA `2f719be07e54362969a43abf20f8ab22b9c3b111`. The fixture's overall
result is `failed` with exit code `1` (the process exit code) — the
generator does not launder that away.

The `checkout-smoke` check keeps its full record in `pr-evidence.json` and
`evidence/generated/summary.md`:

- result: `failed`, exit code: `1`
- risk: "High: checkout cannot complete in the smoke scenario."
- reviewer action: "Block merge until the smoke failure is explained and corrected."
- rollback: "Do not deploy; if already deployed, revert the checkout change."
- artifact copied byte-for-byte to `evidence/generated/artifacts/checkout-smoke.txt`
  and SHA-256 hashed in the JSON record.

The two passing checks (`unit-tests`, `ui-screenshot`) are recorded the same
way, so the failed check is neither hidden nor singled out for special
handling — the generator applies one rule to every entry in the fixture.
